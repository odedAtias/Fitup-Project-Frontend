import {StyleSheet, Text, View} from 'react-native';
// Welcome component
const Welcome = () => {
	return (
		<View>
			<Text>Welcome</Text>
		</View>
	);
};

export default Welcome;

const styles = StyleSheet.create({});
