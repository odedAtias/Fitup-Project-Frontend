import {StyleSheet, Text, View} from 'react-native';

const Feedbacks = () => {
	return (
		<View>
			<Text>Feedbacks</Text>
		</View>
	);
};

export default Feedbacks;

const styles = StyleSheet.create({});
